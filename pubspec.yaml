name: q_amap_demo
description: Demo host app for the q_amap federated AMap Flutter plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  q_amap_flutter:
    path: ../q_amap_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
